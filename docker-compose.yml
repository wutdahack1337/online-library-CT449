services:
  mongo:
    image: mongo
    container_name: mongo-quanlymuonsach
    hostname: mongo-quanlymuonsach
    command: ["--replSet", "rs0"]
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: